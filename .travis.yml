sudo: required

services:
- docker

install: true

script:
- docker build -t slidewiki/learninglocker2-app:"$TRAVIS_COMMIT" app
- if [ "$TRAVIS_PULL_REQUEST" == "false" ] && ( [ "$TRAVIS_BRANCH" == "master" ] || [ "$TRAVIS_BRANCH" == "develop" ] ); then
    docker login -u="$DOCKERHUB_USERNAME" -p="$DOCKERHUB_PASSWORD";
    docker push slidewiki/learninglocker2-app:"$TRAVIS_COMMIT";
    fi

